[core]
    pager = sh -c 'command -v delta >/dev/null 2>&1 && delta "$@" || less -F -X -R "$@"' --
[interactive]
    diffFilter = sh -c 'command -v delta >/dev/null 2>&1 && delta --color-only "$@" || cat "$@"' --
[delta]
    navigate = true
    light = false
    side-by-side = true
