#!/usr/bin/env sh

HISTFILE="${HISTFILE:-${HOME}/.zsh_history}"

cp "${HISTFILE}" "${HISTFILE}".pre-dedup \
  && cat -n "${HISTFILE}" | sort -t ';' -uk2 | sort -nk1 | cut -f2- \
    > "${HISTFILE}".dedup \
  && mv "${HISTFILE}".dedup "${HISTFILE}"